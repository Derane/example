				CREATE TABLE IF NOT EXISTS Student
								(
								    id         INTEGER PRIMARY KEY,
								    number_group             INTEGER,
								    firstname TEXT,
								    lastname  TEXT,
								    birthdate DATE,
								    adress     TEXT,
								    average_grade               INTEGER
								);